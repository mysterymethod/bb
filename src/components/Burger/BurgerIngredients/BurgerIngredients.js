import React from 'react';

import classes from './BurgerIngredients.Module.css';

const burgerIngredients (props) => {
    let ingredients = null;

    switch (props.type) {
        case 'BreadBottom':
            ingredients = <div className={}></div>
            
            break;
    
        default:
            break;
    }

}
